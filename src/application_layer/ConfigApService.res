let getIsDebug = () => {
  ConfigDoService.getIsDebug()
}

let setIsDebug = isDebug => {
  ConfigDoService.setIsDebug(isDebug)
}

let getTransformCount = () => {
  ConfigDoService.getTransformCount()
}

let setTransformCount = transformCount => {
  ConfigDoService.setTransformCount(transformCount)
}

let getGeometryCount = () => {
  ConfigDoService.getGeometryCount()
}

let setGeometryCount = geometryCount => {
  ConfigDoService.setGeometryCount(geometryCount)
}

let getGeometryPointCount = () => {
  ConfigDoService.getGeometryPointCount()
}

let setGeometryPointCount = geometryPointCount => {
  ConfigDoService.setGeometryPointCount(geometryPointCount)
}

let getPBRMaterialCount = () => {
  ConfigDoService.getPBRMaterialCount()
}

let setPBRMaterialCount = pbrMaterialCount => {
  ConfigDoService.setPBRMaterialCount(pbrMaterialCount)
}

let getDirectionLightCount = () => {
  ConfigDoService.getDirectionLightCount()
}

let setDirectionLightCount = directionLightCount => {
  ConfigDoService.setDirectionLightCount(directionLightCount)
}
