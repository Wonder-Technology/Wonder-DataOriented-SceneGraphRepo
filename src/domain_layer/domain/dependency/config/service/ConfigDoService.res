let getIsDebug = () => {
  ConfigRepo.getIsDebug()
}

let setIsDebug = isDebug => {
  ConfigRepo.setIsDebug(isDebug)
}

let getTransformCount = () => {
  ConfigRepo.getTransformCount()
}

let setTransformCount = transformCount => {
  ConfigRepo.setTransformCount(transformCount)
}

let getGeometryCount = () => {
  ConfigRepo.getGeometryCount()
}

let setGeometryCount = geometryCount => {
  ConfigRepo.setGeometryCount(geometryCount)
}

let getGeometryPointCount = () => {
  ConfigRepo.getGeometryPointCount()
}

let setGeometryPointCount = geometryPointCount => {
  ConfigRepo.setGeometryPointCount(geometryPointCount)
}

let getPBRMaterialCount = () => {
  ConfigRepo.getPBRMaterialCount()
}

let setPBRMaterialCount = pbrMaterialCount => {
  ConfigRepo.setPBRMaterialCount(pbrMaterialCount)
}

let getDirectionLightCount = () => {
  ConfigRepo.getDirectionLightCount()
}

let setDirectionLightCount = directionLightCount => {
  ConfigRepo.setDirectionLightCount(directionLightCount)
}
